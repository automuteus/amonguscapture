<UserControl x:Class="AUCapture_WPF.Controls.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters1="clr-namespace:AUCapture_WPF.Converters"
             mc:Ignorable="d" 
             x:Name="PlayerControlWindow"
             d:DesignHeight="80" d:DesignWidth="45">
    <UserControl.Resources>
        <converters1:PlayerToImage x:Key="PlayerToImage" />
    </UserControl.Resources>
    <Grid>

        <Viewbox Stretch="Uniform">
            <Grid Margin="0 0 0 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="4*" />
                </Grid.RowDefinitions>
                <Image Grid.Row="1" Margin="0 0 0 10" Stretch="Uniform">
                    <Image.Source>
                        <MultiBinding Converter="{StaticResource PlayerToImage}">  
                            <Binding ElementName="PlayerControlWindow" Path="Color"></Binding>  
                            <Binding ElementName="PlayerControlWindow" Path="AliveStatus"></Binding>  
                        </MultiBinding>
                    </Image.Source>
                </Image>
                <Viewbox Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center"
                         VerticalAlignment="Center" Margin="0">
                    <Label HorizontalAlignment="Center" Padding="0" Margin="0" VerticalAlignment="Center"
                           FontFamily="{StaticResource AUFont}" Content="{Binding ElementName=PlayerControlWindow, Path=PlayerName}" FontSize="74"
                           FontStyle="Normal" />
                </Viewbox>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
